Terminator = CR LF;

getOutput {
  out "HTR? \$1";
  in "%e";
}

getRange {
  out "RANGE? \$1";
  in "%d";
}

setRange {
  out "RANGE \$1 %d";
}

setMaxTemp {
  out "TLIMIT \$1,%f";
}

getMaxTemp {
  out "TLIMIT? \$1";
  in "%e,%*e,%*e,%*e,%*e";
}